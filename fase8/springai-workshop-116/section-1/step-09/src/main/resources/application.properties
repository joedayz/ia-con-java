spring.application.name=springai-workshop-116-step-09

server.port=8080

# Anthropic (chat + tools)
spring.ai.anthropic.api-key=${ANTHROPIC_API_KEY}
spring.ai.anthropic.chat.options.model=claude-haiku-4-5
spring.ai.anthropic.chat.options.temperature=1.0
spring.ai.anthropic.chat.options.max-tokens=1000

# Embeddings (RAG de step-06)
spring.ai.ollama.base-url=${OLLAMA_BASE_URL:http://localhost:11434}
spring.ai.ollama.embedding.options.model=nomic-embed-text
spring.autoconfigure.exclude=org.springframework.ai.model.ollama.autoconfigure.OllamaChatAutoConfiguration

app.rag.location=classpath:rag/
app.rag.max-segment-size=100
app.rag.max-overlap-size=25
app.rag.max-results=3

# H2 (equivalente a PostgreSQL + Dev Services en Quarkus)
spring.datasource.url=jdbc:h2:mem:miles_of_smiles;DB_CLOSE_DELAY=-1
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

logging.level.org.springframework.ai=INFO
# MCP client — servidor weather en step-08-mcp-server (puerto 8081)
spring.ai.mcp.client.enabled=true
spring.ai.mcp.client.toolcallback.enabled=true
spring.ai.mcp.client.sse.connections.weather.url=http://localhost:8081

app.guardrail.prompt-injection.threshold=0.7

logging.level.dev.springai.workshop=INFO
