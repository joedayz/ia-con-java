server:
  port: 8091

spring:
  application:
    name: fase5-mcp-provider
  ai:
    mcp:
      server:
        name: fase5-mcp-provider
        version: 1.0.0

management:
  endpoints:
    web:
      exposure:
        include: health,info

